<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <meta http-equiv="content-type" content="text/html; charset=utf-8"/>
    {{#icon}}
    <link rel="icon" href="{{icon}}"/>
    {{/icon}}
    {{#css}}
    <link rel="stylesheet" type="text/css" href="{{.}}"/>
    {{/css}}
</head>
<body>
<header id="banner"><h1>{{title}}</h1></header>
<main>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <table class="table">
                    <thead>
                    <tr>
                        <th>Subject</th>
                        <th>Predicate</th>
                        <th>Object</th>
                    </tr>
                    </thead>
                    <tbody>
                    {{#triples}}
                    <tr>
                        <td>{{subject}}</td>
                        <td><a href="{{predicate}}">{{predicateLabel}}</a></td>
                        <td>{{#objectIsIRI}}<a href="{{object}}">{{objectLabel}}</a>{{/objectIsIRI}}{{^objectIsIRI}}{{objectLabel}}{{/objectIsIRI}}
                        </td>
                    </tr>
                    {{/triples}}
                    </tbody>
                </table>
            </div>
        </div>
</main>
<footer><a href="https://www.trellisldp.org">Trellis Linked Data Server</a></footer>
{{#js}}
<script src="{{.}}"></script>
{{/js}}
</body>
</html>